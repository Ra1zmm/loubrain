# Normalize line endings. Shell scripts must stay LF or the shebang breaks
# on Linux/macOS; PowerShell scripts keep CRLF for Windows editors.
* text=auto eol=lf
*.sh   text eol=lf
*.ps1  text eol=crlf
